<script setup>
/**
 * Обвивка за таблици — каноничната рамка (радиус/бордер) на едно място.
 * Винаги overflow-x-auto: тясна таблица не показва скролбар, а широка
 * (напр. класирането на мобилно) може да се скролва хоризонтално.
 *
 * ВАЖНО: ако обвивката (или компонентът около нея) е дете на grid/flex,
 * сложи `min-w-0` на самата grid/flex клетка — иначе клетката се разпъва
 * до ширината на таблицата и вътрешният скрол не се задейства.
 */
</script>

<template>
    <div class="overflow-x-auto rounded-xl border border-zinc-800">
        <slot />
    </div>
</template>
