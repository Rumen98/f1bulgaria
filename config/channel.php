<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Публикуване в канала
    |--------------------------------------------------------------------------
    |
    | Настройки на изходящия поток към Telegram канала. Тук стоят само
    | нетайните — токенът и chat_id са в config/services.php.
    |
    */

    /*
    | Главен ключ. false спира ВСИЧКО изходящо, без деплой — редовете в
    | `channel_posts` продължават да се трупат и тръгват при включване.
    | Затова спирането е безопасно: нищо не се губи, само се отлага.
    */
    'enabled' => (bool) env('CHANNEL_ENABLED', false),

    /*
    | Пауза между два поста. Telegram позволява 1 съобщение в секунда към
    | един чат; 1200ms дава запас, защото лимитът е burst-базиран и при
    | опиране връща 429 с retry_after от порядъка на десетки секунди.
    */
    'post_sleep_ms' => (int) env('CHANNEL_POST_SLEEP_MS', 1200),

    /*
    | Таван на дължината в ЗНАЦИ СЛЕД парсване на entities. Telegram реже на
    | 4096, но лимитът важи за видимия текст — HTML таговете не се броят.
    | Запасът е за емоджи: те заемат 2 UTF-16 единици, а mb_strlen ги брои
    | за 1, тоест може да подценим дължината.
    */
    'max_message_length' => (int) env('CHANNEL_MAX_MESSAGE_LENGTH', 3800),

    /*
    | Праг за новини: публикуват се само статии с importance_score >= това.
    | Pipeline-ът пуска нови статии на всеки 30 мин — без праг каналът се
    | залива и хората го заглушават, което е необратимо.
    */
    'news_min_importance' => (int) env('CHANNEL_NEWS_MIN_IMPORTANCE', 7),

    /*
    | Колко поста максимум да тръгват в едно пускане на `channel:post`.
    | Пази от наводняване след дълъг престой (напр. изключен канал за ден).
    */
    'batch_limit' => (int) env('CHANNEL_BATCH_LIMIT', 10),

    /*
    | След колко неуспешни опита редът се маркира като провален и спира да
    | се пробва. Постоянните грешки (изгонен бот, грешен chat_id) спират
    | веднага, независимо от този брой.
    */
    'max_attempts' => (int) env('CHANNEL_MAX_ATTEMPTS', 5),

    /*
    | Колко назад във времето се поставят резултати в опашката. Пази канала
    | при първо включване сред сезона: без този прозорец всички изминали
    | сесии биха тръгнали наведнъж.
    */
    'max_backfill_hours' => (int) env('CHANNEL_MAX_BACKFILL_HOURS', 24),

    /*
    | Отлагане на публикуването на резултата от състезание. Стюардите
    | променят класирането след финала, а OpenF1 не пре-скрейпва надеждно
    | (br-g/openf1 issues #420, #428, #407). По-добре късно и вярно.
    */
    'race_result_delay_minutes' => (int) env('CHANNEL_RACE_RESULT_DELAY_MINUTES', 45),

    /*
    | Пилоти, които се дописват под класацията, ако са извън първите десет.
    |
    | Причината е редакционна, не техническа: българската аудитория отваря
    | поста заради Цолов. Ако е 14-и, класацията без него не отговаря на
    | въпроса, заради който човекът е кликнал.
    */
    'highlight_driver_slugs' => ['nikola-tsolov'],

    /*
    | Атрибуция за данните от OpenF1. Лицензът е CC BY-NC-SA 4.0 — „BY"
    | означава, че посочването на източника е задължително, не любезност.
    | Слага се само под постове, чиито данни идват от OpenF1.
    */
    'openf1_attribution' => 'Данни: OpenF1 (CC BY-NC-SA 4.0)',

];
